# 병목 진단 및 해결 시스템 설계

> 작성일: 2026.07.05
> 근거 자료: 이 레포의 개발 이력(자존노트 자동화), 옵시디언 데일리노트 구조, Google Drive 활동 기록(하루의 밀도 운영 자료, 수집한 강의·리서치 문서, 복무 정보)

---

## 1. 관찰된 사실 (타임라인)

| 시기 | 활동 | 성격 |
|---|---|---|
| 2025.09~11 | 필사 모임 「하루의 밀도」 1~3기 운영. 보증금 환급 모델, 참가자 후기 매우 긍정적, 재참여 의사 다수 | **출력** (가장 잘 됐던 것) |
| 2025.12 | 전자책 「나의 행복을 재정의합니다」 제작 | **출력** |
| 2026.01.19 | 육군 입대 (전역 2027.07.18) | 환경 변화 |
| 2026.01 | 자청 AI 글쓰기 강의 정리본, 프롬프트 4종, 후킹 제목 템플릿 30선 수집 | 인풋 |
| 2026.04 | 「카르페디엠 1인 비즈니스」 딥리서치 문서 생성 | 인풋 (읽고 끝남) |
| 2026.06.15 | 자존노트 자동화 시스템 구축 (하루 만에 8커밋) | 준비/도구 |
| 2025.11 이후 | 하루의 밀도 4기 모집 없음. 신청 폼·환급 폼은 그대로 남아 있음 | **출력 중단** |

## 2. 병목 진단

### 한 문장 요약

> **인풋과 준비는 계속 쌓이는데, 완결된 출력이 세상 밖으로 나가는 빈도가 2025년 11월 이후 거의 0에 수렴했다.**

병목은 "아이디어가 없다"도, "실행력이 없다"도 아니다. 하루의 밀도 3개 기수를 혼자 기획·모집·운영·정산까지 완주한 실행력은 이미 증명됐다. 병목은 파이프라인의 **마지막 단계 — 수집 → 소화 → ~~출력~~** 에 있고, 입대 이후 이 단계가 끊긴 상태가 8개월째 유지되고 있다.

### 근거 패턴 3가지

**패턴 1 — 인풋:출력 비율의 역전.**
입대 전(2025년 하반기)에는 출력(챌린지 3기수, 전자책)이 인풋을 앞섰다. 입대 후에는 강의 정리본, 프롬프트 모음, 템플릿, 딥리서치 문서가 쌓이는 동안 외부로 나간 완결물이 없다. 인풋은 소비할 때 성장하는 느낌을 주지만, 자산이 되는 것은 출력뿐이다.

**패턴 2 — 시스템 만들기가 출력을 대체하는 경향 (생산적 미루기).**
자존노트 자동화는 훌륭한 도구다. 그러나 구조를 보면 증상이 드러난다: 기록·수집·분류·대시보드 등 **측정 인프라는 하루 만에 완성**했는데, 정작 핵심인 인사이트 단계(`insight_esteem.py`)는 "프롬프트를 생성해서 수동으로 복붙하세요"로 남아 있다. 측정은 자동화됐지만 의미 생성은 병목으로 남았고, 대시보드의 "Claude 분석" 섹션은 "_아직 분석이 없습니다._" 상태일 가능성이 높다. 도구를 다듬는 일은 출력처럼 느껴지지만 출력이 아니다.

**패턴 3 — 잘 되던 것의 재개 장치 부재.**
하루의 밀도는 검증이 끝난 자산이다(유료 참가자, 환급률, 재참여 의사, 심화과정 수요까지 확인됨). 그런데 "군 복무 중에는 못 한다"가 암묵적 전제가 되어 있다. 실제로는 14일 챌린지 운영의 대부분(문장 선정, 공지, 인증 확인)은 저녁 개인정비 시간에 폰으로 가능한 작업이다. 중단의 원인은 시간 부족이 아니라 **재개를 강제하는 구조의 부재**다.

### 왜 지금이 중요한가

전역(2027.07.18)까지 약 **54주**. 이 기간을 "멈춘 시간"으로 정의하면 54주 동안 인풋만 쌓이고, "주 1회 출력 기회 54번"으로 정의하면 전역 시점에 완결물 54개 — 재개된 커뮤니티, 콘텐츠 아카이브, 다음 기수 대기자 명단 — 를 들고 나온다. 같은 시간, 정의만 다르다.

---

## 3. 해결 시스템: 주간 출력 시스템 (Weekly Ship)

### 설계 원칙

1. **단위는 '주 1회 출력'.** 매일이 아니다. 군 일정의 변동성(훈련, 근무)을 흡수하려면 주 단위가 맞다.
2. **출력의 정의는 "내 손을 떠나 타인이 볼 수 있는 완결물".** 스레드/블로그 글 1편, 하루의 밀도 공지 1건, 필사 문장 큐레이션 1세트, 전자책 개정 1챕터 공개, 뉴스레터 1통. 메모, 정리, 계획, 도구 개선은 출력이 아니다.
3. **새 인프라를 만들지 않는다.** 이미 있는 파이프라인(데일리노트 → 섹션 수집 → 대시보드)을 그대로 재사용한다. 이 시스템 자체가 "패턴 2"의 재발이 되지 않도록, 추가되는 것은 스크립트 1개뿐이다.
4. **기록은 폰에서 끝나야 한다.** 데일리노트에 한 줄 쓰는 것이 전부. 스크립트 실행은 PC를 쓸 수 있는 날 주 1회면 충분하다.

### 구조

```
데일리노트 (매일, 폰에서)
 └─ ## 출력          ← 그날 밖으로 내보낸 것이 있으면 한 줄 기록
        ↓  sync_output.py (주 1회 실행)
 출력노트.md          ← 날짜별 아카이브 (자존노트.md와 동일한 형식)
 출력 대시보드.md      ← 이번 주 달성 여부 · 연속 출력 주 · 전역까지 남은 출력 기회
```

### 운영 루틴

| 주기 | 행동 | 소요 |
|---|---|---|
| 매일 저녁 | 데일리노트에 자존노트 기록 (기존 습관 유지). 출력한 게 있으면 `## 출력` 아래 한 줄 추가 | 1분 |
| 주 1회 (일요일 권장) | 이번 주 출력물 1개를 실제로 발행. 가장 만만한 것: 그 주 자존노트/필사 문장 중 하나를 스레드 글로 | 30~60분 |
| 주 1회 (PC 사용 가능일) | `run_output.bat` 실행 → 대시보드 확인 | 1분 |

### 가드레일 (안티패턴 방지 규칙)

- **도구 개선 금지 조항**: 이 레포의 스크립트를 고치는 것은 3회 연속 출력을 달성하기 전에는 금지. 시스템을 다듬고 싶은 충동이 들면 그 에너지로 글을 1편 쓴다.
- **인풋 허가제**: 새 강의·전자책·리서치는 "직전 주에 출력 1개를 완료한 경우"에만 소비한다. 출력이 인풋의 티켓이다.
- **최소 기준 고정**: 바쁜 주에는 "필사 문장 1개 + 내 생각 3줄을 스레드에 올리기"가 출력으로 인정된다. 기준을 낮춰서라도 연속 기록을 지키는 것이 원칙 (하루의 밀도 참가자들에게 이미 가르치고 있는 원리와 같다).

### 첫 4주 부트스트랩 (제안)

1. **1주차**: 하루의 밀도 3기까지의 후기 중 좋은 문장들을 모아 스레드 글 1편 — "복무 중에도 이 모임을 다시 열려는 이유"
2. **2주차**: 필사 문장 7개 큐레이션 + 한 줄 코멘트 발행 (4기 콘텐츠 재고 겸용)
3. **3주차**: 「하루의 밀도」 4기 사전 알림 폼 공개 (기존 구글폼 복사, 날짜 미정으로 — 수요 확인이 목적)
4. **4주차**: 사전 알림 결과를 바탕으로 4기 일정 공지 또는 대기자 대상 미니 챌린지

---

## 3.5 스레드 초안 파이프라인 (출력 마찰 제거 장치)

스레드를 키우지 않았던 진짜 이유: "프롬프트 넣고 받은 글을 그대로 올리는 게 현타 왔다 — 내 이야기가 아니니까." 정확한 진단이다. 해결책은 AI를 빼는 게 아니라 **재료를 바꾸는 것**이다.

```
매일 밤 (폰, 이미 하는 습관)         매일 새벽 05:10 (자동)              저녁 (폰, 5~10분)
데일리노트에 기록                →   draft_threads.py 가              →   초안 3개 중 하나를 골라
(자존노트, 배운 것, 생각)            내 기록만 재료로                      내 말로 고쳐서 발행
                                   스레드 초안 3개 생성                  → 데일리노트 ## 출력에 한 줄
                                   → 볼트 '스레드 초안/' 폴더            → 대시보드 ✅
```

핵심 규칙 (프롬프트에 강제됨):
- **기록에 없는 것은 쓰지 않는다.** 일반론 조언 글 금지. 기록이 빈 날은 초안도 없다 — 오늘 밤의 기록 한 줄이 내일 아침의 초안이 된다.
- **내 문장을 최대한 그대로 살린다.** 각 초안에 `[재료: 원문]` 출처가 붙어, 어떤 내 기록에서 나왔는지 항상 보인다.
- **고르고 고치는 것은 내 몫이다.** 선택과 수정이 곧 저자의 일이다. 셋 다 버리는 것도 선택이다.

사용법: `setup_threads_scheduler.ps1` 로 매일 05:10 자동 실행 등록 (또는 `run_threads.bat` 수동 실행). Claude Code CLI 필요. CLI 호출이 실패하면 프롬프트가 볼트에 저장되므로 폰의 Claude 앱에 붙여넣어도 된다.

## 4. 함께 발견·수정된 버그 (중요)

이번 작업 중 기존 자동화가 **처음부터 동작하지 않고 있었음**을 발견했다. 진단(패턴 2)을 그대로 증명하는 사례다 — 시스템은 만들어졌지만, 실제로 돌아가는지 확인된 적이 없었다.

1. **설정 파일 읽기 크래시**: `config.ini`의 `daily_format = %Y-%m-%d`의 `%` 기호가 configparser 보간 문법과 충돌해 `sync_esteem.py`·`backfill_esteem.py`가 설정을 읽는 순간 예외로 종료. 매일 05:00 스케줄러는 조용히 실패하고 있었다. → `interpolation=None`으로 수정.
2. **헤딩 정규식이 절대 매치되지 않음**: `rf"^#{1,6}\s+..."`에서 f-string이 `{1,6}`을 튜플로 평가해 정규식이 `^#(1, 6)`이 됨. 설령 1번을 통과했더라도 `## 자존노트` 헤딩을 찾지 못해 항상 "기록된 내용이 없습니다"로 끝났을 것. → `{{1,6}}`으로 이스케이프 수정 (5개 파일 전부).

수정 후 모의 볼트로 전 과정(수집 → 아카이브 → 대시보드 → 재실행 멱등성)을 검증 완료. **PC를 쓸 수 있는 날 `backfill_esteem.py`를 한 번 실행하면 그동안 쌓인 데일리노트의 자존노트 기록이 전부 소급 반영된다.**

## 5. 설치 및 사용법

1. `config.ini`의 `[output]` 섹션 확인 (기본값 그대로면 수정 불필요)
2. 데일리노트 템플릿에 `## 출력` 헤딩 추가
3. 주 1회 `run_output.bat` 실행 (또는 `python sync_output.py`)
4. 옵시디언에서 `출력 대시보드.md` 확인

기존 자존노트 동기화(`sync_esteem.py`, 매일 05:00 스케줄러)는 그대로 유지된다. 두 시스템은 독립적으로 동작한다.
